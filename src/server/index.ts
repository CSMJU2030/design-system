/**
 * โค้ดฝั่ง server เท่านั้น — import จาก "@csmju2030/design-system/server"
 * 🔴 ห้าม import ไฟล์นี้จาก Client Component (มี "use client") เพราะพึ่ง next/server
 */
export { createCsmjuAuthRoutes, type CsmjuAuthRoutesOptions } from "./auth-routes";
